{
  "name": "y-m-apps-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && python tools/build_app_manual.py",
    "build:manual": "python tools/build_app_manual.py",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy --project-name=y-m-apps-site --branch=main --commit-dirty=true --commit-message=deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3",
    "wrangler": "^4.95.0"
  }
}
